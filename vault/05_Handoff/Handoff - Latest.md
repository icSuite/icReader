# Handoff - Latest

Last updated: 2026-07-26
Repository snapshot: `main` at `e64206e`
Latest source snapshot: `7b6d293` plus the preserved dirty source path

## Project state

`icReader` is the lightweight reader for conductance and spline products
generated by `icBuilder`; the user confirms that its code/classes are used in
`icAnalyzer`. The project is complete for now and should be revisited later.
No revisit date, priority, or trigger is confirmed.

The vault modernization is documentation-only. It does not change the reader,
generated metadata, cache, example products, or downstream behavior.

## Live repository snapshot

At the start of the 2026-07-26 vault migration:

- committed `main` was aligned with `origin/main` at `7b6d293`;
- the documentation-only vault migration was committed locally at `e64206e`,
  leaving `main` one commit ahead of `origin/main` before the synchronization
  protocol was added;
- the worktree already contained changes in `icreader/splineimage.py`,
  `icreader/__pycache__/splineimage.cpython-310.pyc`, and
  `icreader.egg-info/SOURCES.txt`;
- the source change added `SplineImage.get_geo_grids()` but was not committed;
- `ConductanceImage` and `SplineImage` were the exported public classes;
- tracked conductance and spline example NetCDF files were present;
- all eight live Python files passed a read-only AST syntax parse, including the
  uncommitted source state;
- no automated tests or CI workflow were found;
- package dependencies and README example paths were incomplete.

Inspect Git and live code again when the project is reopened. This note is
orientation, not authority.

## Next action when revisited

1. Resolve the intent and disposition of the three pre-existing dirty paths.
2. Confirm the `icBuilder` product schema and `icAnalyzer` call sites that must
   remain compatible.
3. Declare and reconstruct the real runtime dependency environment.
4. Establish non-writing smoke checks for one conductance and one spline file.
5. Decide whether `SplineFactorImage.py` remains supported.
6. Repair README and manual-example path drift only after the supported
   workflows are confirmed.

## Risks and verification limits

- Imports or editable installation can alter tracked cache or package metadata.
- Manual scripts overwrite tracked figures or write to hard-coded paths.
- The current source worktree has a pre-existing whitespace issue.
- Existing example data do not by themselves prove compatibility with every
  published product or downstream analyzer state.
- No functional reader, schema, or downstream-compatibility test was run
  during the vault migration.

## Portfolio impact

- Central update needed: No
- Changes: None
- Sync summary: `icReader` remains completed for now with no recorded deadline.
  The next portfolio decision remains the trigger and compatibility scope for
  a future revisit.

## Entry points

- `README.md`
- `icreader/__init__.py`
- `icreader/conductanceimage.py`
- `icreader/splineimage.py`
- `example_data/conductance/`
- `example_data/spline/`
- `02_Algorithm/Reader Interfaces and Data Contract.md`

## Historical evidence

The dated notes at the `vault/` root are preserved as legacy session
history. Read them only when investigating a specific earlier spline decision,
configuration, or result.

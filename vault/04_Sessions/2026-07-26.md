# 2026-07-26 — Project-vault modernization

## Scope

Modernize the existing `log/icReader/` research log to the shared project-vault
architecture and register the project in the central second brain.

## Evidence reviewed

- User statements that the project is complete for now, should be revisited,
  reads `icBuilder` products, and is used by `icAnalyzer`.
- Committed `main` at `7b6d293`, aligned with `origin/main`.
- The three pre-existing dirty paths and their targeted diffs.
- Root README, package metadata, exported reader classes, example-data layout,
  manual scripts, Git history, and historical notes.
- Read-only syntax parsing of Python source.

## Documentation changes

- Added root repository instructions and vault onboarding.
- Added conventions, project brief, current state, interface, decision,
  session, and latest-handoff notes.
- Preserved the established vault name and legacy dated history.
- Changed Git policy so local `.obsidian/` workspace state is ignored while
  project memory remains tracked.

No reader implementation, package-generated metadata, cache, NetCDF product,
figure, scientific result, or downstream project was changed or validated.
